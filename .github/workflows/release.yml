name: Disabled Maven Central Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Release workflow disabled on fork branch
    runs-on: ubuntu-latest
    steps:
      - run: echo "Maven Central publishing is intentionally disabled for this fork branch."
